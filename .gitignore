.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Local Claude Code tooling/config
.claude/

# Source data with customer PII (emails) — text is used on the site, file stays local
beast_testimonials.json

# Heavy stock photos kept off the theme branch (Shopify 50MB limit).
# Full set preserved on the 'design-reference' branch and on disk locally.
_design-reference/Images/
